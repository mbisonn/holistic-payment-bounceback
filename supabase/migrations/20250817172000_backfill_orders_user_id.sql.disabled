-- Backfill orders.user_id by matching customer_email to auth.users.email
update public.orders o
set user_id = u.id
from auth.users u
where o.user_id is null
  and o.customer_email is not null
  and lower(o.customer_email) = lower(u.email);
